# Core
pyautogui
numpy
opencv-python
Pillow

# Screen capture
mss
wincam; sys_platform == "win32"

# YOLO object detection
ultralytics

# OCR (spectate_capture OCR mode, auto_connect --mode ocr)
easyocr

# Fuzzy string matching
rapidfuzz

# Global keyboard listener (kill switch)
pynput
